Checking 51 chains...

anonymize:
  ! Export `anonymizeSpec` not mentioned in README
  ! Export `mapInstructions` not mentioned in README
  ! Export `filterInstructions` not mentioned in README
  ! Export `reduceInstructions` not mentioned in README
  ! Export `findInstructions` not mentioned in README
  ! Export `groupInstructions` not mentioned in README
  ! Export `applyAnonymization` not mentioned in README
  ! Export `createAnonymizer` not mentioned in README
  - Uses shared config params but doesn't reference shared configuration docs
  ! Has spec pattern (`anonymizeSpec`) but it's not in README

category-samples:
  ! Export `buildSeedGenerationPrompt` not mentioned in README
  ! Export `categorySamplesList` not mentioned in README
  ~ Config param `count` not mentioned in README
  ~ Config param `diversityLevel` not mentioned in README
  ~ Config param `retryDelay` not mentioned in README
  - Uses shared config params but doesn't reference shared configuration docs

central-tendency:
  ! Export `centralTendencyRetry` not mentioned in README
  - Uses shared config params but doesn't reference shared configuration docs

collect-terms:
  - Uses shared config params but doesn't reference shared configuration docs

date:
  - Uses shared config params but doesn't reference shared configuration docs

detect-patterns:
  ~ Config param `maxStringLength` not mentioned in README
  ~ Config param `maxArrayLength` not mentioned in README
  - Uses shared config params but doesn't reference shared configuration docs

document-shrink:
  - Uses shared config params but doesn't reference shared configuration docs

expect:
  ! Export `aiExpect` not mentioned in README
  ! Export `expectSimple` not mentioned in README

extract-blocks:
  ~ Config param `windowSize` not mentioned in README
  - Uses shared config params but doesn't reference shared configuration docs

extract-features:
  - Uses shared config params but doesn't reference shared configuration docs

filter-ambiguous:
  ~ Config param `chunkSize` not mentioned in README
  - Uses shared config params but doesn't reference shared configuration docs

find:
  - Uses shared config params but doesn't reference shared configuration docs

intersections:
  ~ Config param `useSchemaValidation` not mentioned in README
  - Uses shared config params but doesn't reference shared configuration docs

list:
  - Uses shared config params but doesn't reference shared configuration docs

llm-logger:
  ! Export `initLogger` not mentioned in README
  ! Export `createHostLoggerIntegration` not mentioned in README
  ~ Config param `hostLogger` not mentioned in README

people:
  - Uses shared config params but doesn't reference shared configuration docs

pop-reference:
  - Uses shared config params but doesn't reference shared configuration docs

questions:
  - Uses shared config params but doesn't reference shared configuration docs

relations:
  - Uses shared config params but doesn't reference shared configuration docs

scale:
  - Uses shared config params but doesn't reference shared configuration docs

tag-vocabulary:
  ~ Config param `problematicSampleSize` not mentioned in README
  - Uses shared config params but doesn't reference shared configuration docs

test:
  - Uses shared config params but doesn't reference shared configuration docs

test-analysis:
  ~ No README.md

test-analyzer:
  ~ No README.md

themes:
  ~ Config param `chunkSize` not mentioned in README
  ~ Config param `topN` not mentioned in README
  - Uses shared config params but doesn't reference shared configuration docs

timeline:
  - Uses shared config params but doesn't reference shared configuration docs

to-object:
  - Uses shared config params but doesn't reference shared configuration docs

veiled-variants:
  ! Export `scientificFramingPrompt` not mentioned in README
  ! Export `causalFramePrompt` not mentioned in README
  ! Export `softCoverPrompt` not mentioned in README

--- Summary ---
Clean: 23
Issues: 28

High-severity issues: 19
